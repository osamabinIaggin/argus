# Live streaming pipeline — Phase 1 (ingest) dependencies.
# Installed into a dedicated python3.12 venv (NOT system python 3.14, which the
# ML stack — torch/ultralytics — does not yet have wheels for).
#
#   python3.12 -m venv .venv
#   ./.venv/bin/pip install -r streaming/requirements.txt

av>=16,<17          # PyAV — FFmpeg bindings for decode (devices + network streams)
numpy>=2.1          # frame arrays

# Dev
pytest>=8           # drop-stale / reconnect unit tests

# --- Tier 1 (detection + tracking) ---
# ultralytics       # YOLO26 + ByteTrack  (needs python<=3.13)
# torch             # Tier 1 backend (MPS on Apple Silicon)

# --- Tier 2 (gated VLM, optional — default backend is FastVLM on MLX) ---
# Install on an Apple Silicon Mac to enable `--vlm fastvlm`:
#   ./.venv/bin/pip install mlx-vlm pillow timm
# mlx-vlm  — MLX inference for VLMs (Apple Silicon only)
# pillow   — frame ndarray -> PIL image for the model
# timm     — pulled in by FastVLM's custom processor code (torch already present)
# The stub backend (`--vlm stub`) and all tests need NONE of the above.
